chai = require 'chai'
sinon = require 'sinon'

# {ConveyorBase,ConveyorUtil} = require '../src/base'
{Conveyor} = require '../src/conveyor'

chai.should()

describe 'conveyor', ->
  it 'should do something', ->
    true.should.equal true
  it 'should do something else', ->
    true.should.equal true